import { CustomFilterProps } from '@/types'
import React from 'react'

const CustomFilter = ({}:CustomFilterProps) => {
  return (
    <div>
      CustomFilter
    </div>
  )
}

export default CustomFilter
